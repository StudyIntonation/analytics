analyticsApp {
    pgclient {
        host = "localhost"
        port = 5432
        user = "postgres"
        password = "postgres"
        database = "studyintonation-analytics"

        pool {
            maxIdleTimeMillis = 1000
            maxSize = 15
        }
    }
}
